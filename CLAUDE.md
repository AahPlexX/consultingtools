# Claude Repository Instructions

`AGENTS.md` is the universal agent entry point for this repository. Read it first, then follow the `governance/` SSOT directory in its mandated order.

Do not create a Claude-specific policy fork. If a governing rule must change, update the applicable file under `governance/` and its verification coverage so every model inherits the same rule.
